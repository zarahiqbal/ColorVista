/** Login screen: remember email on this device (never stores password). */
export const REMEMBER_LOGIN_KEY = "@ColorVista_rememberLogin";

/** When "true", Firebase session survives app restarts until explicit logout. */
export const PERSIST_SESSION_KEY = "@ColorVista_persistSession";
